import 'package:flutter/material.dart';

class ArrowIngredient extends StatelessWidget {
  const ArrowIngredient({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
